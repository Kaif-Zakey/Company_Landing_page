# Backend Environment Variables Example
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=5001

# MongoDB Configuration - Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name

# Optional: Development settings
NODE_ENV=development

# CORS Configuration (optional)
FRONTEND_URL=http://localhost:5174

# Cloudinary Configuration (optional - for backend image processing)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Instructions:
# 1. Replace MONGODB_URI with your actual MongoDB connection string
# 2. Change 'username' and 'password' to your actual MongoDB credentials
# 3. Change 'cluster' to your actual MongoDB cluster name
# 4. Change 'database-name' to your actual database name
# 5. Update PORT if you want to use a different port
# 6. Replace Cloudinary values with your actual credentials if needed
# 7. Restart the server after making changes to the .env file
